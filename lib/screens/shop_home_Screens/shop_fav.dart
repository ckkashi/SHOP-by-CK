import 'package:flutter/material.dart';

class ShopFav extends StatelessWidget {
  const ShopFav({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}